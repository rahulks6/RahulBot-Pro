{
  "name": "@katkee/mobile",
  "version": "0.1.0",
  "private": true,
  "description": "KATKEE mobile app (Phase 1: design system, navigation shell, authentication)",
  "scripts": {
    "start": "react-native start",
    "android": "react-native run-android",
    "ios": "react-native run-ios",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-native": "0.75.4",
    "@react-navigation/native": "^6.1.18",
    "@react-navigation/native-stack": "^6.11.0",
    "@react-navigation/bottom-tabs": "^6.6.1",
    "react-native-screens": "^3.34.0",
    "react-native-safe-area-context": "^4.11.0",
    "@react-native-async-storage/async-storage": "^1.24.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/react": "^18.3.5",
    "@react-native/typescript-config": "^0.75.4"
  },
  "katkeeNotes": {
    "networkSandboxLimitation": "This session's environment blocks registry.npmjs.org, so none of the above can be installed and this app cannot be built or run here (React Native also needs Xcode/Android SDK, which no cloud sandbox provides anyway — device testing was always going to happen on real hardware per the project spec). This is real, hand-written TypeScript source, not generated scaffolding, but it has not been typechecked or run in this session. Run `npm install` (or `yarn`) on a machine with React Native's toolchain set up, then `npm run typecheck` before trusting it further."
  }
}
