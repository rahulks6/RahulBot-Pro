/** User-facing preconditions for speech generation (shared by the queue and the audio pipeline). */
export const NARRATOR_VOICE_NEEDED =
  'Narration needs a narrator voice: create one under Characters → Voices (role: narrator), then choose it as "Narrator voice" in the project settings.';
